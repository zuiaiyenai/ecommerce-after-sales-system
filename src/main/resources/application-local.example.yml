spring:
  datasource:
    username: root
    password: your-local-mysql-password

pgvector:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/ecommerce_rag
    username: ecommerce
    password: ecommerce_pgvector
    driver-class-name: org.postgresql.Driver

app:
  jwt:
    secret: "change-this-secret-key-to-at-least-32-bytes-in-production"
