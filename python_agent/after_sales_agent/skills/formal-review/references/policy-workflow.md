# 政策检索工作流

1. 只使用 Java 提供的商家、政策版本、商品类目、售后类型、业务时间和上下文版本作为可信过滤条件。
2. 使用严格过滤检索 `after_sales_policy`。覆盖不足且基础设施正常时，最多执行一次受限多查询改写。
3. 只有商家、政策版本、生效时间、来源类型、严格过滤级别、阈值和可追溯引用全部通过时，才把命中视为权威政策。
4. 只提取权威政策明确支持的凭证要求。
5. 输出 `PolicyAssessment`，不得审批、驳回、请求补证或修改业务状态，不得绕过确定性 Gate 或 Java 持久化边界。
