[pytest]
pythonpath = .
addopts = -m "not real_llm"
markers =
    real_llm: calls a configured real remote LLM and is skipped by default
    integration: uses an isolated local container or service
    smoke: small controlled environment verification
